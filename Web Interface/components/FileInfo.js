import React, { useCallback, useState } from 'react'

export default function FileInfo({gcode}) {

    return (
        <div className='bg-blue-500 h-full w-full p-2'>
            Hello
        </div>
    )
}